{
    "name": "postech-financeiro-mfe",
    "version": "0.0.1",
    "private": true,
    "packageManager": "npm@10.9.8",
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "dev": "turbo run dev --parallel",
        "build": "turbo run build",
        "lint": "turbo run lint",
        "typecheck": "turbo run typecheck"
    },
    "engines": {
        "node": ">=22.0.0"
    },
    "devDependencies": {
        "turbo": "^2.3.3"
    }
}